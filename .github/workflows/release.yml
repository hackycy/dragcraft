name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@v1
    with:
      build: pnpm build:packages
      publish: true
    permissions:
      contents: write
      id-token: write
