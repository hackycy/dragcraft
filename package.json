{
  "type": "module",
  "version": "0.0.8",
  "private": true,
  "packageManager": "pnpm@11.6.0",
  "sideEffects": false,
  "scripts": {
    "build": "turbo build",
    "build:packages": "turbo build --filter='./packages/**'",
    "check:public-boundary": "node scripts/check-public-boundary.mjs",
    "dev": "turbo dev",
    "docs:dev": "pnpm --dir docs dev",
    "docs:build": "pnpm --dir docs build",
    "docs:preview": "pnpm --dir docs preview",
    "play": "pnpm build:packages && pnpm -F ./playground dev",
    "lint": "eslint --cache . && pnpm check:public-boundary",
    "prepublishOnly": "pnpm build",
    "release": "bumpp -r",
    "skills:check": "node skills/check.mjs",
    "skills:test": "vitest run skills/check.test.mjs",
    "test": "pnpm build:packages && pnpm -r test",
    "test:browser": "pnpm build:packages && playwright test",
    "typecheck": "pnpm build:packages && tsc",
    "prepare": "simple-git-hooks"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:cli",
    "@antfu/utils": "catalog:inlined",
    "@playwright/test": "catalog:testing",
    "@tsdown/css": "catalog:cli",
    "@types/node": "catalog:types",
    "bumpp": "catalog:cli",
    "eslint": "catalog:cli",
    "fast-check": "catalog:testing",
    "lint-staged": "catalog:cli",
    "publint": "catalog:cli",
    "simple-git-hooks": "catalog:cli",
    "tinyexec": "catalog:testing",
    "tsdown": "catalog:cli",
    "tsx": "catalog:cli",
    "turbo": "catalog:cli",
    "typescript": "catalog:cli",
    "vite": "catalog:cli",
    "vitest": "catalog:testing",
    "vitest-package-exports": "catalog:testing",
    "yaml": "catalog:testing"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm i --frozen-lockfile --ignore-scripts --offline && npx lint-staged"
  },
  "lint-staged": {
    "*": "eslint --fix"
  }
}
